# --- MLS automation ---
HAR_USERNAME=
HAR_PASSWORD=

# --- Geocoding provider selection ---
# Options: census | geocodio | mapbox | google | maptiler
GEOCODER_PROVIDER=census

# Optional keys for non-Census providers (leave blank if unused)
MAPTILER_API_KEY=
MAPBOX_TOKEN=
GOOGLE_MAPS_KEY=
GEOCODIO_API_KEY=

# --- Playwright/browser ---
HEADLESS=False# HAR / Matrix
HAR_USERNAME=
HAR_PASSWORD=

# Map / Geocoding
MAPTILER_API_KEY=
GEOCODER_PROVIDER=census     # census | geocodio | mapbox | google

# Census tuning (optional overrides)
CENSUS_BENCHMARK=Public_AR_Current
CENSUS_VINTAGE=Current_Current
CENSUS_RATE_LIMIT_PER_MIN=300

# Runtime toggles
HEADLESS=False
HTTP_PROXY=
HTTPS_PROXY=

